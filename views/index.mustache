<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap">
    <link rel="stylesheet" href="stylesheets/tailwind/tailwind.min.css">
    <link rel="stylesheet" href="stylesheets/main.css">
    <link rel="icon" type="image/png" sizes="16x16" href="favicon-tailwind.png">
</head>
<body class="antialiased bg-body text-body font-body">
<div>
    {{>navbar}}

    <section class="relative py-20 lg:pt-32 bg-blue-800 overflow-x-hidden">
        <!-- BACKGROUND SWIRLS -->
        <img class="hidden md:block absolute top-0 left-0" src="assets/elements/white-line.svg" alt="">
        <img class="absolute top-50 left-0 mt-64" src="assets/elements/orange-line.svg" alt="">
        <img class="absolute bottom-0 right-0 w-72" src="assets/elements/violet-line.svg" alt="">

        <div class="relative container mx-auto px-4">
            <h2 class="mb-16 text-4xl md:text-5xl text-white font-bold font-heading">The Most Popular Products</h2>
            <div class="flex flex-wrap -mx-3">
                {{#products}}
                    <div class="w-full lg:w-1/3 px-3 mb-16 lg:mb-0">
                        <a class="block mb-10" href="/product">
                            <!-- SALE STICKER -->
                            <div class="relative">
                                <span class="absolute bottom-0 left-0 ml-6 mb-6 px-2 py-1 text-xs font-bold font-heading bg-white border-2 border-red-500 rounded-full text-red-500">-15.33%</span>
                                <img class="w-full h-96 object-cover"
                                     src="assets/images/placeholder-playing-tennis.png" alt="">
                            </div>
                            <div class="mt-12">
                                <!-- PRODUCT INFO -->
                                <div class="mb-2">
                                    <h3 class="mb-3 text-3xl text-white font-bold font-heading">{{name}}</h3>
                                    <p class="text-xl font-bold font-heading text-white">
                                        {{#sale_price}}
                                            <span>${{sale_price}}</span>
                                            <span class="text-xs text-gray-500 font-semibold font-heading line-through">${{price}}</span>
                                        {{/sale_price}}
                                        {{^sale_price}}
                                            <span>${{price}}</span>
                                        {{/sale_price}}
                                    </p>
                                </div>
                            </div>
                        </a>
                        <!-- ADD TO CART -->
                        <a class="inline-block bg-orange-300 hover:bg-orange-400 text-white font-bold font-heading py-4 px-8 mb-10 rounded-md uppercase transition duration-200"
                           href="#">Add to Cart</a>
                    </div>
                {{/products}}
            </div>
        </div>
    </section>
</div>

{{>footer}}
</body>
</html>